projects:
  Ticketing:
    schema: ./schema.ticketing.graphql
    include: '**/*.graphql'
    extensions:
      endpoints:
        ticketservice:
          url: http://localhost:5108/graphql/
          introspect: true
